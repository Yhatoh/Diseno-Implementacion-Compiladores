NAME: letrec - rec with three lbds
DESCRIPTION: recursion with three lbds
SRC:
(letrec
  ((even (lambda (n) (if (<= n 0) true (@ odd (+ n -1)))))
    (odd  (lambda (n) (if (<= n 0) false (@ even (+ n -1)))))
    (fact (lambda (n) (if (<= n 1) 1 (* n (@ fact (- n 1)))))))
  (@ odd (@ fact 2)))
EXPECTED:
|ORACLE