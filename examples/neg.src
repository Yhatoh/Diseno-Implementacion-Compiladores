(
  (record oth2)
  (record bot abc x y z)
  (def (g x) (if (<= x 5) 5 x))
  (def (f x y) (let (h 2) (+ (+ 2 x) (g y))))
  (let (x true)
    (let (y 10)
      (let (z (bot (tup 2 false (tup)) -20 (oth2) true))
        (if (and (not (<= y (* -1 (bot-x z)))) x)
          (<= (+ y (add1 (bot-abc (set z 0 (get (set (tup (tup)) 0 1) 0))))) 20)
          (and (<= y (print 30)) x)))))
)