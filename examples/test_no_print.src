(let (f (lambda (x y)
            (let (nothing (@ y))
                (@ x 42))))
    (let (tupcreate (lambda () 
                        (let (t (tup 1 2))
                            5)))
        (let (ct (@ tupcreate))
              (let (ans (@ f (lambda (x) x) tupcreate))
              ans)
              )
          ))